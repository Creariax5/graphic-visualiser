package org.example;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

public class tmp {
    /*Circle cell = cells.get(id);

            x = cell.getX();
            y = cell.getY();

            calcX = x - 700;
            calcY = y - 500;
            System.out.println(calcX + " " + calcY);
            double distanceToC = Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));

            calcX = (1 * calcX) / distanceToC;
            calcY = (1 * calcY) / distanceToC;

            x = x - calcX;
            y = y - calcY;*/
}
